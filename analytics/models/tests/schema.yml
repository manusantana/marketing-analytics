version: 2
models:
  - name: stg_products
    columns:
      - name: id
        tests: [not_null, unique]
  - name: stg_customers
    columns:
      - name: id
        tests: [not_null, unique]
  - name: stg_orders
    columns:
      - name: id
        tests: [not_null, unique]
